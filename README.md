# Pixel Valentine Popup

This is a tiny static HTML/CSS/JS project. To run it locally, start a static server from the repo root and open `index.html` in your browser.

## Quick start

```bash
npm run dev
```

Then visit http://localhost:8000.

## Other options

```bash
python -m http.server 8000
```

